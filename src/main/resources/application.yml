## YAML Template.
---
server:
  port: 3100

spring:
  datasource:
    url: jdbc:postgresql://travelinv-postgre.postgres.database.azure.com:5432/travelinv-postgre
    username: azure_admin
    password: Baitaplonso7
    driver-class-name: org.postgresql.Driver
  security:
    user:
      name: admin
      password: admin
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
